# Nav To Cart

**URL:** /

Navigating from the homepage to the Cart page via the header cart link. With no items added, the cart should advertise itself as empty and link back to the catalog.

> Generated by `/crawl-site` — structural overview. Review and edit before feeding into `/scenario-to-tests`.

## Test 1: Click the 'Cart' header link

- **Action:** Click the "Cart 0" link in the page header.
- **Expected:** The URL changes to `/cart`.
- **Expected:** The page title is "Cart · Bookstore Demo".
- **Expected:** The heading "Your Cart" (h1) is visible.
- **Expected:** The text "Your cart is empty." is visible.
- **Expected:** A "Browse the catalog →" link pointing to `/catalog` is visible.
